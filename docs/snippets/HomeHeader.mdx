export const HomeHeader = ({ title, children }) => {
  return (
    <header className="home_header block not-prose w-full my-2 ring-2 ring-transparent  dark:shadow-none overflow-hidden py-6 text-black dark:text-white">
      <h1 className="text-3xl font-semibold mb-2">{title}</h1>
      <div className="text-base leading-relaxed">{children}</div>
    </header>
  );
};
